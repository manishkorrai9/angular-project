import { Route } from '@angular/router';
import { RequestListComponent } from './request-list.component';

export const RequestListRoutes: Route[] = [
    {
        path     : '',
        component: RequestListComponent,
    }
];
