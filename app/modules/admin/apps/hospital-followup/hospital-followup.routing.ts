import { Route } from '@angular/router';

import { HospitalFollowupComponent } from './hospital-followup.component';

export const HospitalFollowupRoutes: Route[] = [
    {
        path     : '',
        component: HospitalFollowupComponent,
    }
];
